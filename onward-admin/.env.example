# Shared Onward API — the admin and the customer frontend use the SAME backend,
# so any change made in the admin is reflected on the frontend.
VITE_API_BASE=/api
VITE_API_PROXY=http://localhost:4000
